import React, { useState } from 'react'

const UseHook = () => {
    const [data,setdata]=useState({});
    const products=use(fetch("")
    .then(res=>res.json()));

  return (
    <div>
      
    </div>
  )
}

export default UseHook
